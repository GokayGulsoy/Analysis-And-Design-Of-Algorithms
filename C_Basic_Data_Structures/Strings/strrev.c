#include <stdio.h>
#include <string.h>

int main()
{
  
  char str[20];
  printf("\n Enter any string: ");
  scanf("%s",str);
  
  // strrev function is not supported by strrev() function
  //printf(" Rversed string is: %s",strrev(str));

  return 0; 
}